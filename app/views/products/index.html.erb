<h1>All products</h1>

<% @products.each do |p| %>
    <p>
      <%= p.name %>
      <br />
      <% image = p.name.downcase.gsub(' ', '_') %>
      <%= image_tag("#{image}.jpeg", alt: "#{ p.name }") %>
    </p>
<% end %>
