<h1><%= @category.name %></h1>

<% @products.each do |p| %>
  <p>Name: <%= link_to "#{ p.name }", product_path(p) %>, $<%= p.price %></p>
  <% image = Product.find_photo_image(p) %>
  <%= link_to image_tag(image, alt: "#{ p.name }"), product_path(p) %>
<% end %>
