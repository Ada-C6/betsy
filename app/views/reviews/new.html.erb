<h2>Review <%= capitalize_each_word(@product.name) %></h2>

<%= render partial: "form",
            locals: {
              http_method: :post,
              local_path: product_reviews_path
              } %>

<%= link_to "Back to Product", product_path(@product) %>
